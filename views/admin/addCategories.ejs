<% if(admin.staff_No === 1) { %>
    <%- include('../partials/staff1Header.ejs') %>
<% } else if(admin.staff_No === 2) { %>
    <%- include('../partials/staff2Header.ejs') %>
<% } else if(admin.staff_No === 3) { %>
    <%- include('../partials/staff3Header.ejs') %>
<% } else { %>
    <%- include ('../partials/adminHeader.ejs') %>
<% } %>
 

        <h4 class="text-center text-success my-5"> ADD CATEGORIES</h4>
        <h4 class="text-center text-danger my-1">
            <%=message%>
        </h4>

        <form id="myForm" action="/admin/addCategories" method="post">
            <!-- Email input -->
            <div class=" mb-4 text-center">
                
                <input style="margin-left: 25%; margin-right: 30%;" type="text" id="addCategories" name="addCategory"
                    class="form-control form-control-lg border-white w-50 h-10 " placeholder="Enter your category name"
                    required />
                <span id="Email"></span>
                
            </div>

            
            <div style="display: flex; justify-content: space-between;">
                <a style="margin-left: 40%;"><button type="submit" class="btn-success rounded-2 p-2">Save</button></a>
        </form>
        <a style="margin-right: 40%;" href="/admin/category"><button type="button"
                class="btn-primary rounded-2 p-2">cancel</button></a>

    </div>

    <script>
        let category = document.getElementById("addCategories");
        let message = document.getElementById("message");

        // document.getElementById("saveButton").addEventListener("click", function(event) {
    
            // Check if the input length is less than 12 characters
            if (category.length < 12) {
            // Submit the form
            console.log("inside");
            document.getElementById("myForm").submit();
            } else {
                event.preventDefault();
            // Add your custom logic here if the input length is not less than 12 characters
            message.innerHTML="Input length should be less than 12 characters"
            console.log("Input length is not less than 12 characters");
    }
//   });

        

    </script>
    <%- include ('../partials/adminFooter.ejs') %>